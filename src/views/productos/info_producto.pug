extends ../layout
block append config

  -paginaActual = producto.nombre || animal.nombre
  block append
    link(rel="stylesheet", href="/styles/disponible/animal-info.css")

    script(src="https://js.stripe.com/v3/")
block main

  br
  section.animal.-Menu-margen.-Padd2
    div.carta.-Padd.-Flex.-Shadow.-Padd2
      img.carta__img.-Borde(src=producto.path || animal.path ,alt=producto.nombre || animal.nombre)
      br
      br
      div.carta__info
        h2.nombre= producto.nombre || animal.nombre
        br
        h4.sub PRECIO
        p= producto.precio || animal.precio
        br
        h4= producto.descripcion || animal.descripcion
        br
        h4.sub CATEGORIA
        h4= producto.seccion || animal.seccion
        br
        br
        //aqui va el boton de stripe
        form(action='/compra/'+(producto.id || animal.id) method='POST')
          script.stripe-button(
            src='https://checkout.stripe.com/checkout.js' 
            data-key='pk_test_vszwAbDx8zPg1AZrZn6Ec4Lr00i0d7OeHj'
            data-amount= (producto.precio*100 || animal.precio*100)
            data-name='ÚNICO REPTILES'
            data-description=(producto.descripcion || animal.descripcion)
            data-image='/img/logo.png'
            data-locale='auto'
            data-currency='mxn'
          )